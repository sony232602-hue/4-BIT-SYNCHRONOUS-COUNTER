# 4-Bit-Synchronous-Counter
A 4-bit synchronous counter is a binary counter in which all four flip-flops receive the same clock signal. Therefore, all flip-flops change state simultaneously on the active clock edge.
